version: '3.8'

services:
  api:
    build: .
    image: test-backend:latest
    container_name: test-backend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
